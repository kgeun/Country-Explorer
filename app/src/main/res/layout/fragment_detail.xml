<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="country"
            type="com.kgeun.countryexplorer.data.model.network.CECountryViewItem" />
    </data>

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/primary_light_grey_1">
        
        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/appBarLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            
            <com.google.android.material.appbar.CollapsingToolbarLayout
                android:id="@+id/collapingToolbar"
                android:layout_width="match_parent"
                android:layout_height="@dimen/top_head_height"
                android:background="@color/primary_light_grey_1"
                app:contentScrim="@color/basic_black_0.4"
                app:layout_scrollFlags="scroll|exitUntilCollapsed|snap">

                <FrameLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:layout_collapseMode="parallax">

                    <ImageView
                        android:id="@+id/main.imageview.placeholder"
                        android:layout_width="match_parent"
                        android:layout_height="@dimen/top_head_background_height"
                        android:scaleType="fitXY"
                        android:src="@drawable/splash_image" />

                    <FrameLayout
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:background="@color/solid_grey_0.5" />
                </FrameLayout>

                <FrameLayout
                    android:id="@+id/fl_background"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:fitsSystemWindows="true"
                    app:layout_collapseMode="parallax"
                    app:layout_collapseParallaxMultiplier="0.7" />

                <androidx.appcompat.widget.Toolbar
                    android:id="@+id/toolbar"
                    android:layout_width="match_parent"
                    android:layout_height="?attr/actionBarSize"
                    app:layout_collapseMode="pin"
                    app:popupTheme="@style/ThemeOverlay.AppCompat.Light" />

                <FrameLayout
                    android:id="@+id/stuff_container"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent">

                    <TextView
                        android:id="@+id/title"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_gravity="center_vertical"
                        android:gravity="center"
                        android:text="@{country.name}"
                        android:textAllCaps="true"
                        android:textColor="@android:color/white"
                        android:textSize="14sp"
                        android:visibility="invisible" />

                    <ImageView
                        android:id="@+id/imgb_avatar_wrap"
                        android:layout_width="wrap_content"
                        android:layout_height="@dimen/default_expanded_image_size"
                        android:layout_gravity="bottom|center_horizontal"
                        android:adjustViewBounds="true"
                        app:setThumbnailUrl="@{country.flag}" />

                </FrameLayout>

                <androidx.appcompat.widget.AppCompatTextView
                    android:id="@+id/titleToolbarTextSingle"
                    android:layout_width="wrap_content"
                    android:layout_height="?attr/actionBarSize"
                    android:layout_gravity="center_horizontal"
                    android:ellipsize="end"
                    android:gravity="center"
                    android:scrollHorizontally="true"
                    android:singleLine="true"
                    android:text="@{country.name}"
                    android:textAllCaps="true"
                    android:textColor="@color/basic_black"
                    android:textSize="17sp"
                    android:visibility="invisible"
                    app:layout_collapseMode="pin"
                    tools:visibility="visible" />

                <androidx.appcompat.widget.AppCompatTextView
                    android:id="@+id/titleToolbarText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center_horizontal"
                    android:layout_margin="@dimen/avatar_margin"
                    android:ellipsize="marquee"
                    android:gravity="center"
                    android:maxLines="2"
                    android:scrollHorizontally="true"
                    android:text="@{country.name}"
                    android:textAllCaps="true"
                    android:textColor="@color/basic_black"
                    android:textSize="24sp"
                    app:layout_collapseMode="pin"
                    tools:visibility="visible" />
            </com.google.android.material.appbar.CollapsingToolbarLayout>
        </com.google.android.material.appbar.AppBarLayout>
    </androidx.coordinatorlayout.widget.CoordinatorLayout>
</layout>